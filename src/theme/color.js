const Colors = {
  WHITE: "#FFFFFF",
  BLACK: "#000000",
};

const lightColors = {
  BACKGROUND: Colors.WHITE,
  TEXT:Colors.BLACK
};
const DarkColors = {
  BACKGROUND: Colors.BLACK,
  TEXT:Colors.WHITE
};

export const color = { Colors, lightColors, DarkColors };

